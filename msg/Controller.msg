# The types here are contained in the ros package std_msgs
# Request parameters:
bool 						full_verbosity 	# Print also the controller gain
std_msgs/MultiArrayLayout 	layout 			# specification of data layout
float64[]         			controller_gain # array of data